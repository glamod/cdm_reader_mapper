name: cdm-test

channels:
    - conda-forge
    - nodefaults

dependencies:
    - pandas
    - dask
    - matplotlib
    - xarray
    - bottleneck
    - netcdf4
    - h5netcdf
    - datashader
    # for testing
    - pytest
    - pytest-cov
    - pytest-env
    #from pypi
    - pip:
      - msgpack
      - requests
      - timezonefinder
      - python-slugify
      - textdistance
      - swifter
      - platformdirs
